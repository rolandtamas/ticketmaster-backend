#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ng-swagger-gen/ng-swagger-gen" "$@"
  ret=$?
else 
  node  "$basedir/../ng-swagger-gen/ng-swagger-gen" "$@"
  ret=$?
fi
exit $ret
